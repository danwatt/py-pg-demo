[project]
name = "pg-demo"
version = "0.1.0"
description = "PostgreSQL demos to static site via MkDocs"
requires-python = ">=3.13"
dependencies = [
  "psycopg2-binary>=2.9,<3",
  "mkdocs>=1.6,<2",
  "Faker>=37,<38",
  "PyYAML>=6,<7",
    "mkdocs-material"
]
